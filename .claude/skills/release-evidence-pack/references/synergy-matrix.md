# Release Evidence Synergy Matrix

The authoritative routing table lives at `.claude/rules/02-agent-synergy.md`. `/ship`, `/review`, and `/audit` must cite that file instead of maintaining divergent specialist-selection rules.
